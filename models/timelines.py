# timeline struct
'''
Struct = {
    id: string;
    events: [
      {
        id: string;
        creator: string // should be the id of the user o created the event
        date: string;
        time: string;
        title: string;
        description: string;
        assigned: string[]
      }
    ]
}
'''
timelines = []
